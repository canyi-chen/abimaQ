% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gen_data_quantile_mediation.R
\name{gen_data_quantile_mediation}
\alias{gen_data_quantile_mediation}
\title{Generate Synthetic Data for Quantile Mediation Analysis}
\usage{
gen_data_quantile_mediation(
  n = 200,
  alpha = 0,
  beta = 1,
  gamma = 1,
  beta_X = c(0.5, 0.2, 0.2),
  beta_M = c(0.8, 0.3, 0.3, 0.4),
  beta_Y = c(-0.2, 0.4, -0.2, 0.7),
  sigmas = rep(0.3, 2),
  rho_W = 0.2
)
}
\arguments{
\item{n}{Sample size (default \code{200}).}

\item{alpha}{Path coefficient from X to M on the latent scale.}

\item{beta}{Path coefficient from M to Y on the latent scale.}

\item{gamma}{Path coefficient from X to Y on the latent scale.}

\item{beta_X}{Coefficients for W in the X model.}

\item{beta_M}{Coefficients for W in the M model.}

\item{beta_Y}{Coefficients for W in the Y model.}

\item{sigmas}{Standard deviations for X and M margins.}

\item{rho_W}{Compound symmetry correlation for W.}
}
\value{
A data frame with columns \code{X}, \code{M}, \code{Y} and \code{W}.
}
\description{
Simulates data under a Gaussian copula structural equation model with
Gaussian margins for exposure (X) and mediator (M), and Gamma (Exponential) margins for outcome (Y).
The design matches the data generating process in the paper for validating the AB test under the null.
}
\examples{
set.seed(1)
dat <- gen_data_quantile_mediation(n = 500, alpha = 0, beta = 1, gamma = 1)
head(dat)
}
